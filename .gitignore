node_modules
.next
out
build
.DS_Store
*.pem
.env*
!.env.example
.vercel
next-env.d.ts
tsconfig.tsbuildinfo
coverage
test-results
playwright-report
.playwright

# Cloudflare / OpenNext build output
.open-next
.wrangler

# Local agent tooling and working agreement — not part of the published site
CLAUDE.md
.claude/

# Source resume. Nothing imports it — the site reads the typed files in
# content/*.ts — and it still carries private drafting notes (team size to fill
# in, degree to add, certifications to consider). Not for a public repo.
content/resume.md
